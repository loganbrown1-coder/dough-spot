export default function LogoMark({ className }: { className?: string }) {
  // eslint-disable-next-line @next/next/no-img-element
  return <img src="/brand/opspot-icon.png" alt="OpSpot" className={className} />;
}
